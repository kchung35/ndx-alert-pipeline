# NDX 100 · Factor × Options × Insider Alert Pipeline
# Tested with Python 3.13; 3.11 and 3.12 should work.

yfinance>=1.2.0
pandas>=2.0
numpy>=1.26
requests>=2.30
lxml>=5.0
pyarrow>=15.0
scipy>=1.11
streamlit>=1.30
plotly>=5.18

# Windows only — provides IANA timezone data for zoneinfo.
# macOS and Linux ship it in the system already.
tzdata; platform_system == "Windows"
